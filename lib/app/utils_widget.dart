import 'package:flutter/material.dart';

class Utils extends StatelessWidget {
  const Utils({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class AppTheme {
  static const cBlack = Colors.black;
  static const cWhite = Colors.white;
  static const cRed = Colors.red;
  static const cBlue = Colors.blue;
  static const cGrey = Colors.grey;
}
