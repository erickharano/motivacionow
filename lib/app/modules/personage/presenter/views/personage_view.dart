import 'package:flutter/material.dart';

class PersonageView extends StatelessWidget {
  const PersonageView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Card();
  }
}
